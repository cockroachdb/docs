/**
 * Directory Data File for all content
 *
 * This replicates Jekyll's defaults configuration:
 * defaults:
 *   - scope:
 *       path: ''
 *       type: pages
 *     values:
 *       layout: page
 */

module.exports = {
  // Default layout for all pages (can be overridden in individual frontmatter)
  layout: "page"
};
