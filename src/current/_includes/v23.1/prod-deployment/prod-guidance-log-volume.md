Store CockroachDB [log files]({% link {{ page.version.version }}/configure-logs.md %}#logging-directory) in a separate volume from the main data store so that logging is not impacted by I/O throttling.