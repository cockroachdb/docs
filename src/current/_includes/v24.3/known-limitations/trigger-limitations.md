- `CREATE OR REPLACE TRIGGER` is not supported. #128422
- Statement-level triggers are not supported. #126362
- `INSTEAD OF` triggers are not supported. #126363
- A [trigger function]({% link {{ page.version.version }}/triggers.md %}#trigger-function) that is used in an existing trigger cannot be replaced with `CREATE OR REPLACE` syntax. To use `CREATE OR REPLACE`, first [drop any triggers]({% link {{ page.version.version }}/drop-trigger.md %}) that are using the function. #134555
- Hidden columns are not visible to triggers. #133331
- {% include {{ page.version.version }}/known-limitations/drop-trigger-limitations.md %}