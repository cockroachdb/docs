Determine where CockroachDB <a href="{% link {{ page.version.version }}/configure-logs.md %}#logging-directory">log files</a> will be stored: either on the same volume as the main data store or on a separate volume from the main data store. Refer to <a href="{% link {{ page.version.version }}/logging-best-practices.md %}#storage-considerations-for-file-sinks">Storage considerations for file sinks</a> in Logging Best Practices.