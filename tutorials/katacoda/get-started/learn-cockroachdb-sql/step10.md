When you no longer need a table, use [`DROP TABLE`](https://www.cockroachlabs.com/docs/stable/drop-table.html) followed by the table name to remove the table and all its data:

```sql
DROP TABLE drivers;
```{{execute}}
