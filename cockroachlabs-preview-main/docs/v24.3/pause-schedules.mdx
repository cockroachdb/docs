---
title: "PAUSE SCHEDULES"
old_description: "The PAUSE SCHEDULES statement lets you temporarily halt the process of a backup schedule."
---

import { PauseSchedules4 } from "/snippets/syntax-diagrams/PauseSchedules4.jsx";

The `PAUSE SCHEDULES` [statement](/docs/v24.3/sql-statements) can be used to pause [backup schedules](/docs/v24.3/create-schedule-for-backup) and [changefeed schedules](/docs/v24.3/create-schedule-for-changefeed).

After pausing a schedule, you can resume it with [`RESUME SCHEDULES`](/docs/v24.3/resume-schedules).

## Required privileges

The following users can pause a schedule:

- Members of the [`admin` role](/docs/v24.3/security-reference/authorization#default-roles). By default, the `root` user belongs to the `admin` role.
- Owners of a backup schedule, i.e., the user that [created the backup schedule](/docs/v24.3/create-schedule-for-backup).
- Owners of a changefeed schedule, i.e., the user that [created the changefeed schedule](/docs/v24.3/create-schedule-for-changefeed).

## Synopsis

<PauseSchedules4 />

## Parameters

<table><thead><tr><th>Parameter</th><th>Description</th></tr></thead><tbody><tr><td><code>selectclause</code></td><td>A <a href="/docs/v24.3/selection-queries">selection query</a> that returns <code>id</code>(s) to pause.</td></tr><tr><td><code>scheduleID</code></td><td>The <code>id</code> of the schedule you want to pause, which can be found with <a href="/docs/v24.3/show-schedules"><code>SHOW SCHEDULES</code></a>.</td></tr></tbody></table>

## Examples

### Pause a single schedule

```sql
> PAUSE SCHEDULE 589963390487363585;
```

```text
PAUSE SCHEDULES 1
```

### Pause multiple schedules

To pause multiple schedules, nest a [`SELECT` clause](/docs/v24.3/select-clause) that retrieves `id`(s) inside the `PAUSE SCHEDULES` statement:

```sql
> PAUSE SCHEDULES WITH x AS (SHOW SCHEDULES) SELECT id FROM x WHERE label = 'schedule_database';
```

```text
PAUSE SCHEDULES 4
```

In this example, all schedules with the label `schedule_database` are paused.

## See also

- [Manage a Backup Schedule](/docs/v24.3/manage-a-backup-schedule)
- [`BACKUP`](/docs/v24.3/backup)
- [`RESTORE`](/docs/v24.3/restore)
- [`CREATE CHANGEFEED`](/docs/v24.3/create-changefeed)
- [`SHOW BACKUP`](/docs/v24.3/show-backup)
- [`SHOW SCHEDULES`](/docs/v24.3/show-schedules)
- [`RESUME SCHEDULES`](/docs/v24.3/resume-schedules)
- [`DROP SCHEDULES`](/docs/v24.3/drop-schedules)
- [`PAUSE JOB`](/docs/v24.3/pause-job)
- [`RESUME JOB`](/docs/v24.3/pause-job)
- [`CANCEL JOB`](/docs/v24.3/cancel-job)
- [Take Full and Incremental Backups](/docs/v24.3/take-full-and-incremental-backups)
- [Use the Built-in SQL Client](/docs/v24.3/cockroach-sql)
- [`cockroach` Commands Overview](/docs/v24.3/cockroach-commands)
