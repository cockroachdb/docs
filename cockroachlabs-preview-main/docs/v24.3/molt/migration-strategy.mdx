---
title: "Migration Strategy"
old_description: "Build a migration strategy before performing a database migration to CockroachDB."
---

import MigrationStrategy from "/snippets/molt/migration-strategy.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v24.3");
      });
    });
  }
  return null;
};

<PatchVersion />
<MigrationStrategy />
