---
title: "Egress Perimeter Controls for CockroachDB Advanced"
old_description: "Learn how to configure Egress Perimeter Controls for enhanced network security on a CockroachDB Cloud cluster."
---

import EgressPerimeterControls from "/snippets/cockroachcloud/egress-perimeter-controls.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v24.3");
      });
    });
  }
  return null;
};

<PatchVersion />
<EgressPerimeterControls />
