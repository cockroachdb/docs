---
title: "Stream a Changefeed to Snowflake"
old_description: "Use a CockroachDB Cloud cluster to stream changefeed messages to a Snowflake cluster."
---

import StreamChangefeedToSnowflakeAws from "/snippets/cockroachcloud/stream-changefeed-to-snowflake-aws.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v24.3");
      });
    });
  }
  return null;
};

<PatchVersion />
<StreamChangefeedToSnowflakeAws />
