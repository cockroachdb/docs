---
title: "Understand the Resource Usage of your CockroachDB Standard Cluster"
old_description: "Diagnose your RU consumption and tune your workload to reduce costs"
---

import ResourceUsage from "/snippets/cockroachcloud/resource-usage.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v24.3");
      });
    });
  }
  return null;
};

<PatchVersion />
<ResourceUsage />
