---
title: "Backup and Restore Monitoring"
old_description: "An overview of backup and restore monitoring features for CockroachDB Cloud deployments."
---

import BackupAndRestoreMonitoring from "/snippets/cockroachcloud/backup-and-restore-monitoring.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v24.3");
      });
    });
  }
  return null;
};

<PatchVersion />
<BackupAndRestoreMonitoring />
