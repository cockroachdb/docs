---
title: "Release Support Policy"
old_description: "Learn about Cockroach Labs' policy for supporting major releases of CockroachDB."
---

import ReleaseSupportPolicy from "/snippets/releases/release-support-policy.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v24.3");
      });
    });
  }
  return null;
};

<PatchVersion />
<ReleaseSupportPolicy />
