---
title: "Sessions Page"
old_description: "The Sessions page provides details of all open sessions in the cluster."
---

import SessionsPage from "/snippets/cockroachcloud/sessions-page.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v24.1");
      });
    });
  }
  return null;
};

<PatchVersion />
<SessionsPage />
