---
title: "Authentication on CockroachDB Cloud"
old_description: "Learn about the authentication features for CockroachDB Cloud clusters."
---

import Authentication from "/snippets/cockroachcloud/authentication.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v24.1");
      });
    });
  }
  return null;
};

<PatchVersion />
<Authentication />
