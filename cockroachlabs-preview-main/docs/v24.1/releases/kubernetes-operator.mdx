---
title: "Public Kubernetes Operator Releases"
old_description: "Changelog for the Kubernetes Operator for CockroachDB"
---

import KubernetesOperator from "/snippets/releases/kubernetes-operator.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v24.1");
      });
    });
  }
  return null;
};

<PatchVersion />
<KubernetesOperator />
