---
title: "Migration Overview"
old_description: "Learn how to migrate your database to a CockroachDB cluster."
---

import MigrationOverview from "/snippets/molt/migration-overview.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v24.1");
      });
    });
  }
  return null;
};

<PatchVersion />
<MigrationOverview />
