---
title: "CockroachDB Cloud Releases"
old_description: "Changelog for CockroachDB Cloud."
---

import Cloud from "/snippets/releases/cloud.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v23.2");
      });
    });
  }
  return null;
};

<PatchVersion />
<Cloud />
