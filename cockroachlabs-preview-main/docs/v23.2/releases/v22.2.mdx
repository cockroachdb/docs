---
title: "What's New in v22.2"
old_description: "Additions and changes in CockroachDB version v22.2 since version v22.1"
---

import V22_2 from "/snippets/releases/v22.2.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v23.2");
      });
    });
  }
  return null;
};

<PatchVersion />
<V22_2 />
