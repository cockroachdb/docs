---
title: "CockroachDB Downloads Archive"
old_description: "Archive of downloads for versions of CockroachDB that are no longer supported"
---

import DownloadsArchive from "/snippets/releases/downloads-archive.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v23.2");
      });
    });
  }
  return null;
};

<PatchVersion />
<DownloadsArchive />
