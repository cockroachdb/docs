---
title: "What's New in v24.2"
old_description: "Additions and changes in CockroachDB version v24.2 since version v24.1"
---

import V24_2 from "/snippets/releases/v24.2.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v23.2");
      });
    });
  }
  return null;
};

<PatchVersion />
<V24_2 />
