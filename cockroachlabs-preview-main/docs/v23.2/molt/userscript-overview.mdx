---
title: "Userscript Overview"
old_description: "Learn how userscripts work and how they can customize a MOLT Replicator process."
---

import UserscriptOverview from "/snippets/molt/userscript-overview.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v23.2");
      });
    });
  }
  return null;
};

<PatchVersion />
<UserscriptOverview />
