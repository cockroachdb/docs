---
title: "ALTER SEQUENCE"
old_description: "Use the ALTER SEQUENCE statement to change the name, increment values, and other settings of a sequence."
---

import { AlterSequence2 } from "/snippets/syntax-diagrams/AlterSequence2.jsx";

The `ALTER SEQUENCE` [statement](/docs/v23.2/sql-statements) applies a [schema change](/docs/v23.2/online-schema-changes) to a sequence.

<Note>
The `ALTER SEQUENCE` statement performs a schema change. For more information about how online schema changes work in CockroachDB, see [Online Schema Changes](/docs/v23.2/online-schema-changes).
</Note>

## Required privileges

- To alter a sequence, the user must have the `CREATE` [privilege](/docs/v23.2/security-reference/authorization#managing-privileges) on the parent database.
- To change the schema of a sequence with `ALTER SEQUENCE... SET SCHEMA`, or to change the database of a sequence with `ALTER SEQUENCE... RENAME TO`, the user must also have the `DROP` [privilege](/docs/v23.2/security-reference/authorization#managing-privileges) on the sequence.

## Syntax

<AlterSequence2 />

## Parameters

<table><thead><tr><th>Parameter</th><th>Description</th></tr></thead><tbody><tr><td><code>IF EXISTS</code></td><td>Modify the sequence only if it exists; if it does not exist, do not return an error.</td></tr><tr><td><code>sequence&#95;name</code></td><td>The name of the sequence.</td></tr><tr><td><code>RENAME TO sequence&#95;name</code></td><td>Rename the sequence to <code>sequence&#95;name</code>, which must be unique to its database and follow these <a href="/docs/v23.2/keywords-and-identifiers#identifiers">identifier rules</a>. Name changes do not propagate to the table(s) using the sequence.<br/><br/>Note that <code>RENAME TO</code> can be used to move a sequence from one database to another, but it cannot be used to move a sequence from one schema to another. To change a sequence's schema, use <code>ALTER SEQUENCE ...SET SCHEMA</code> instead.</td></tr><tr><td><code>CYCLE</code>/<code>NO CYCLE</code></td><td>The sequence will wrap around when the sequence value reaches the maximum or minimum value. <code>CYCLE</code> is not implemented. If <code>NO CYCLE</code> is set, the sequence will not wrap.</td></tr><tr><td><code>OWNED BY column&#95;name</code></td><td>Associates the sequence to a particular column. If that column or its parent table is dropped, the sequence will also be dropped.<br/><br/>Specifying an owner column with <code>OWNED BY</code> replaces any existing owner column on the sequence. To remove existing column ownership on the sequence and make the column free-standing, specify <code>OWNED BY NONE</code>.<br/><br/><strong>Default:</strong><code>NONE</code></td></tr><tr><td><code>CACHE</code></td><td>The number of sequence values to cache in memory for reuse in the session. A cache size of <code>1</code> means that there is no cache, and cache sizes of less than <code>1</code> are not valid.<br/><br/><strong>Default:</strong><code>1</code> (sequences are not cached by default)</td></tr><tr><td><code>MINVALUE</code></td><td>The new minimum value of the sequence.<br/><br/><strong>Default:</strong><code>1</code></td></tr><tr><td><code>MAXVALUE</code></td><td>The new maximum value of the sequence.<br/><br/><strong>Default:</strong><code>9223372036854775807</code></td></tr><tr><td><code>INCREMENT</code></td><td>The new value by which the sequence is incremented. A negative number creates a descending sequence. A positive number creates an ascending sequence.</td></tr><tr><td><code>START &#91;WITH&#93;</code></td><td>The value the sequence starts at if you <code>RESTART</code> or if the sequence reaches <code>MAXVALUE</code> and <code>CYCLE</code> is set.</td></tr><tr><td><code>RESTART &#91;WITH&#93;</code></td><td>Sets <code>nextval</code> to the specified number, or back to the original <code>START</code> value.</td></tr><tr><td><code>VIRTUAL</code></td><td>Creates a <em>virtual sequence</em>.<br/><br/>Virtual sequences are sequences that do not generate monotonically increasing values and instead produce values like those generated by the built-in function <a href="/docs/v23.2/functions-and-operators"><code>unique&#95;rowid()</code></a>. They are intended for use in combination with <a href="/docs/v23.2/serial"><code>SERIAL</code></a>-typed columns.</td></tr><tr><td><code>SET SCHEMA schema&#95;name</code></td><td>Change the schema of the sequence to <code>schema&#95;name</code>.</td></tr><tr><td><code>OWNER TO role&#95;spec</code></td><td>Change the owner of the sequence to <code>role&#95;spec</code>.</td></tr></tbody></table>

## Examples

### Change the increment value of a sequence

In this example, we're going to change the increment value of a sequence from its current state (i.e., `1`) to `2`.

```sql
> CREATE SEQUENCE customer_seq;
```

```sql
> SHOW CREATE customer_seq;
```

```text
   table_name  |                                     create_statement
---------------+-------------------------------------------------------------------------------------------
  customer_seq | CREATE SEQUENCE customer_seq MINVALUE 1 MAXVALUE 9223372036854775807 INCREMENT 1 START 1
(1 row)
```

```sql
> ALTER SEQUENCE customer_seq INCREMENT 2;
```

```text
   table_name  |                                        create_statement
---------------+--------------------------------------------------------------------------------------------------
  customer_seq | CREATE SEQUENCE public.customer_seq MINVALUE 1 MAXVALUE 9223372036854775807 INCREMENT 2 START 1
(1 row)
```

### Rename a sequence

In this example, we will change the name of sequence.

```sql
> CREATE SEQUENCE even_numbers INCREMENT 2 START 2;
```

```sql
> SHOW SEQUENCES;
```

```text
  sequence_schema | sequence_name
------------------+----------------
  public          | even_numbers
(1 row)
```

```sql
> ALTER SEQUENCE even_numbers RENAME TO even_sequence;
```

```sql
> SHOW SEQUENCES;
```

```text
  sequence_schema | sequence_name
------------------+----------------
  public          | even_sequence
(1 row)
```

### Change the database of a sequence

In this example, we will move the sequence we renamed in the first example (`even_sequence`) from `defaultdb` (i.e., the default database) to a different database.

```sql
> SHOW SEQUENCES FROM defaultdb;
```

```text
  sequence_schema | sequence_name
------------------+----------------
  public          | even_sequence
(1 row)
```

```sql
> CREATE DATABASE mydb;
```

```sql
> ALTER SEQUENCE even_sequence RENAME TO mydb.even_sequence;
```

```sql
> SHOW SEQUENCES FROM defaultdb;
```

```text
  sequence_schema | sequence_name
------------------+----------------
(0 rows)
```

```sql
> SHOW SEQUENCES FROM mydb;
```

```text
  sequence_schema | sequence_name
------------------+----------------
  public          | even_sequence
(1 row)
```

### Change the schema of a sequence

Suppose you [create a sequence](/docs/v23.2/create-sequence) that you would like to add to a new schema called `cockroach_labs`:

```sql
> CREATE SEQUENCE even_numbers INCREMENT 2 START 2;
```

```sql
> SHOW SEQUENCES;
```

```text
  sequence_schema | sequence_name
------------------+----------------
  public          | even_numbers
(1 row)
```

By default, [unqualified sequences](/docs/v23.2/sql-name-resolution#lookup-with-unqualified-names) created in the database belong to the `public` schema:

```sql
> SHOW CREATE public.even_numbers;
```

```text
      table_name      |                                        create_statement
----------------------+--------------------------------------------------------------------------------------------------
  public.even_numbers | CREATE SEQUENCE public.even_numbers MINVALUE 1 MAXVALUE 9223372036854775807 INCREMENT 2 START 2
(1 row)
```

If the new schema does not already exist, [create it](/docs/v23.2/create-schema):

```sql
> CREATE SCHEMA IF NOT EXISTS cockroach_labs;
```

Then, change the sequence's schema:

```sql
> ALTER SEQUENCE even_numbers SET SCHEMA cockroach_labs;
```

```sql
> SHOW CREATE public.even_numbers;
```

```text
ERROR: relation "public.even_numbers" does not exist
SQLSTATE: 42P01
```

```sql
> SHOW SEQUENCES;
```

```text
  sequence_schema | sequence_name
------------------+----------------
  cockroach_labs  | even_numbers
(1 row)
```

```sql
> SHOW CREATE cockroach_labs.even_numbers;
```

```text
          table_name          |                                            create_statement
------------------------------+----------------------------------------------------------------------------------------------------------
  cockroach_labs.even_numbers | CREATE SEQUENCE cockroach_labs.even_numbers MINVALUE 1 MAXVALUE 9223372036854775807 INCREMENT 2 START 2
(1 row)
```

## See also

- [`CREATE SEQUENCE`](/docs/v23.2/create-sequence)
- [`DROP SEQUENCE`](/docs/v23.2/drop-sequence)
- [`SHOW SEQUENCES`](/docs/v23.2/show-sequences)
- [Functions and Operators](/docs/v23.2/functions-and-operators)
- [SQL Statements](/docs/v23.2/sql-statements)
- [Online Schema Changes](/docs/v23.2/online-schema-changes)
