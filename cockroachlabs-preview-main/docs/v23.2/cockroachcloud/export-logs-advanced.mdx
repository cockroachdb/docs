---
title: "Export Logs From a CockroachDB Advanced Cluster"
old_description: "Export Logs From a CockroachDB Advanced Cluster"
---

import ExportLogsAdvanced from "/snippets/cockroachcloud/export-logs-advanced.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v23.2");
      });
    });
  }
  return null;
};

<PatchVersion />
<ExportLogsAdvanced />
