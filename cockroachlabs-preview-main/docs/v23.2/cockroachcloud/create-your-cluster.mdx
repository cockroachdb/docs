---
title: "Create a CockroachDB Standard Cluster"
old_description: "Learn how to create your CockroachDB Standard cluster."
---

import CreateYourCluster from "/snippets/cockroachcloud/create-your-cluster.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v23.2");
      });
    });
  }
  return null;
};

<PatchVersion />
<CreateYourCluster />
