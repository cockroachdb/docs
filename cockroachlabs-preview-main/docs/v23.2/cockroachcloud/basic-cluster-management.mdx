---
title: "Manage a CockroachDB Basic Cluster"
old_description: "Manage your cluster's schema, data, and more."
---

import BasicClusterManagement from "/snippets/cockroachcloud/basic-cluster-management.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v23.2");
      });
    });
  }
  return null;
};

<PatchVersion />
<BasicClusterManagement />
