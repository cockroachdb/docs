---
title: "Changefeed metrics"
old_description: "The Changefeed metrics let you monitor the performance of your changefeeds."
---

import MetricsChangefeeds from "/snippets/cockroachcloud/metrics-changefeeds.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v23.2");
      });
    });
  }
  return null;
};

<PatchVersion />
<MetricsChangefeeds />
