---
title: "Jobs Page"
old_description: "The Jobs page provides details of all jobs in the cluster."
---

import JobsPage from "/snippets/cockroachcloud/jobs-page.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v23.2");
      });
    });
  }
  return null;
};

<PatchVersion />
<JobsPage />
