---
title: "Export Logs From a CockroachDB Standard Cluster"
old_description: "Export Logs From a CockroachDB Standard Cluster"
---

import ExportLogs from "/snippets/cockroachcloud/export-logs.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v23.2");
      });
    });
  }
  return null;
};

<PatchVersion />
<ExportLogs />
