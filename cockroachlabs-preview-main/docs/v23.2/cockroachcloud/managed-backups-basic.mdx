---
title: "Managed Backups in CockroachDB Basic Clusters"
old_description: "Learn about CockroachDB Cloud managed backups."
---

import ManagedBackupsBasic from "/snippets/cockroachcloud/managed-backups-basic.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v23.2");
      });
    });
  }
  return null;
};

<PatchVersion />
<ManagedBackupsBasic />
