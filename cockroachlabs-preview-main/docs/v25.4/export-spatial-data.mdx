---
title: "Export Spatial Data"
old_description: "Learn how to export spatial data from CockroachDB into various formats."
---

CockroachDB supports efficiently storing and querying spatial data.

This page has instructions for exporting spatial data from CockroachDB and converting it to other spatial formats using the [`ogr2ogr`](https://gdal.org/programs/ogr2ogr) command.

<Note>
An `ogr2ogr` version of 3.1.0 or higher is required to generate data that can be imported into CockroachDB.
</Note>

## Step 1. Export data to CSV

First, use the [`EXPORT`](/docs/v25.4/export) statement to export your data to a CSV file.

In the example statement below, we export the tornadoes database used in [Query Spatial Data](/docs/v25.4/query-spatial-data#use-a-sample-shapefile-dataset).

The statement will place the CSV file in the node's [store directory](/docs/v25.4/cockroach-start#store), in a subdirectory named `extern/tornadoes`. The file's name is automatically generated, and will be displayed as output in the [SQL shell](/docs/v25.4/cockroach-sql).

```sql
EXPORT INTO CSV 'nodelocal://self/tornadoes' WITH nullas = '' FROM SELECT * from "1950-2018-torn-initpoint";
```

```text
                     filename                     | rows  |  bytes
--------------------------------------------------+-------+-----------
  export16467a35d30d25700000000000000001-n1.0.csv | 63645 | 16557064
(1 row)
```

<Note>
This example uses local file storage. For more information about other locations where you can export your data (such as cloud storage), see [`EXPORT`](/docs/v25.4/export).
</Note>

## Step 2. Combine multiple CSV files into one, as needed

You should now have one or more CSV files in the `extern/tornadoes` subdirectory of your node's [store directory](/docs/v25.4/cockroach-start#store). Depending on the size of the data set, there may be more than one CSV file.

To combine multiple CSVs into one file:

1. Open the CSV file where you will be storing the combined output in a text editor. You will need to manually add the CSV header columns to that file so that the `ogr2ogr` output we generate below will have the proper column names. Start by running the statement below on the table you are exporting to get the necessary column names:

    ```sql
    WITH x AS (SHOW COLUMNS FROM "1950-2018-torn-initpoint") SELECT string_agg(column_name, ',') FROM x;
    ```

    ```text
                                                  string_agg
    ------------------------------------------------------------------------------------------------------
      gid,om,yr,mo,dy,date,time,tz,st,stf,stn,mag,inj,fat,loss,closs,slat,slon,elat,elon,len,wid,fc,geom
    ```
2. Add the column names output above to your target output CSV file (e.g., `tornadoes.csv`) as header columns. For the tornadoes database, they should look like the following:

    ```text
    gid, om, yr, mo, dy, date, time, tz, st, stf, stn, mag, inj, fat, loss, closs, slat, slon, elat, elon, len, wid, fc, geom
    ```
3. Concatenate the non-header data from all of the exported CSV files, and append the output to the target CSV file as shown below. The node's store directory on this machine is `/tmp/node0`.

    ```shell
    cat /tmp/node0/extern/tornadoes/*.csv >> tornadoes.csv
    ```

## Step 3. Convert CSV to other formats using `ogr2ogr`

Now that you have your data in CSV format, you can convert it to other spatial formats using [`ogr2ogr`](https://gdal.org/programs/ogr2ogr).

For example, to convert the data to SQL, run the following command:

```shell
ogr2ogr -f PGDUMP tornadoes.sql -lco LAUNDER=NO -lco DROP_TABLE=OFF -oo GEOM_POSSIBLE_NAMES=geom -oo KEEP_GEOM_COLUMNS=off tornadoes.csv
```

Note that the options `-oo GEOM_POSSIBLE_NAMES=<geom_column_name> -oo KEEP_GEOM_COLUMNS=off` are required no matter what output format you are converting into.

For more information about the formats supported by `ogr2ogr`, see the [`ogr2ogr` documentation](https://gdal.org/programs/ogr2ogr).

<Note>
An `ogr2ogr` version of 3.1.0 or higher is required to generate data that can be imported into CockroachDB.
</Note>

Finally, note that SQL type information is lost in the conversion to CSV, such that the `tornadoes.sql` file output by the `ogr2ogr` command above lists every non-geometry field as a [`VARCHAR`](/docs/v25.4/string).

This can be addressed in one of the following ways:

- Modify the data definitions in the SQL output file to use the correct types.
- Run [`ALTER TYPE`](/docs/v25.4/alter-type) statements to restore the data's SQL types after loading this data into another database (including another CockroachDB instance).

## See also

- [`EXPORT`](/docs/v25.4/export)
- [Migrate from Shapefiles](/docs/v25.4/migrate-from-shapefiles)
- [Migrate from GeoJSON](/docs/v25.4/migrate-from-geojson)
- [Migrate from GeoPackage](/docs/v25.4/migrate-from-geopackage)
- [Migrate from OpenStreetMap](/docs/v25.4/migrate-from-openstreetmap)
- [Spatial Data Overview](/docs/v25.4/spatial-data-overview)
- [Spatial indexes](/docs/v25.4/spatial-indexes)
- [Spatial and GIS Glossary of Terms](/docs/v25.4/architecture/glossary)
- [Known Limitations](/docs/v25.4/spatial-data-overview#known-limitations)
- [Spatial functions](/docs/v25.4/functions-and-operators#spatial-functions)
- [POINT](/docs/v25.4/point)
- [LINESTRING](/docs/v25.4/linestring)
- [POLYGON](/docs/v25.4/polygon)
- [MULTIPOINT](/docs/v25.4/multipoint)
- [MULTILINESTRING](/docs/v25.4/multilinestring)
- [MULTIPOLYGON](/docs/v25.4/multipolygon)
- [GEOMETRYCOLLECTION](/docs/v25.4/geometrycollection)
- [Well known text](/docs/v25.4/well-known-text)
- [Well known binary](/docs/v25.4/well-known-binary)
- [GeoJSON](/docs/v25.4/geojson)
- [SRID 4326 - longitude and latitude](/docs/v25.4/srid-4326)
- [`ST_Contains`](/docs/v25.4/st_contains)
- [`ST_ConvexHull`](/docs/v25.4/st_convexhull)
- [`ST_CoveredBy`](/docs/v25.4/st_coveredby)
- [`ST_Covers`](/docs/v25.4/st_covers)
- [`ST_Disjoint`](/docs/v25.4/st_disjoint)
- [`ST_Equals`](/docs/v25.4/st_equals)
- [`ST_Intersects`](/docs/v25.4/st_intersects)
- [`ST_Overlaps`](/docs/v25.4/st_overlaps)
- [`ST_Touches`](/docs/v25.4/st_touches)
- [`ST_Union`](/docs/v25.4/st_union)
- [`ST_Within`](/docs/v25.4/st_within)
