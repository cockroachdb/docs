---
title: "What's New in v23.2"
old_description: "Additions and changes in CockroachDB version v23.2 since version v23.1"
---

import V23_2 from "/snippets/releases/v23.2.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v25.4");
      });
    });
  }
  return null;
};

<PatchVersion />
<V23_2 />
