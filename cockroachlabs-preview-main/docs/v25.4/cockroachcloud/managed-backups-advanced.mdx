---
title: "Managed Backups in CockroachDB Advanced Clusters"
old_description: "Learn about CockroachDB Cloud managed backups."
---

import ManagedBackupsAdvanced from "/snippets/cockroachcloud/managed-backups-advanced.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v25.4");
      });
    });
  }
  return null;
};

<PatchVersion />
<ManagedBackupsAdvanced />
