---
title: "CockroachDB Cloud Regions"
old_description: "Learn about the available regions for clusters in CockroachDB Cloud."
---

import Regions from "/snippets/cockroachcloud/regions.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v25.4");
      });
    });
  }
  return null;
};

<PatchVersion />
<Regions />
