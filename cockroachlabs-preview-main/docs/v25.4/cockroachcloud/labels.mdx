---
title: "Organize CockroachDB Cloud Resources Using Labels"
old_description: "Learn how to use labels to organize CockroachDB Cloud clusters and folders."
---

import Labels from "/snippets/cockroachcloud/labels.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v25.4");
      });
    });
  }
  return null;
};

<PatchVersion />
<Labels />
