---
title: "Manage a CockroachDB Advanced Cluster"
old_description: "Manage your cluster's schema, data, and more."
---

import AdvancedClusterManagement from "/snippets/cockroachcloud/advanced-cluster-management.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v25.4");
      });
    });
  }
  return null;
};

<PatchVersion />
<AdvancedClusterManagement />
