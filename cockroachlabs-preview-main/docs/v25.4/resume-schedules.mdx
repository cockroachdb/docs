---
title: "RESUME SCHEDULES"
old_description: "The RESUME SCHEDULES statement lets you resume jobs that were previously paused with PAUSE SCHEDULE."
---

import { ResumeSchedules6 } from "/snippets/syntax-diagrams/ResumeSchedules6.jsx";

The `RESUME SCHEDULES` [statement](/docs/v25.4/sql-statements) can be used to resume [paused schedules](/docs/v25.4/pause-schedules):

- [Backup schedule](/docs/v25.4/create-schedule-for-backup)
- [Changefeed schedule](/docs/v25.4/create-schedule-for-changefeed)

When a schedule is resumed, the `next_run` will be set to the next `TIMESTAMP` that conforms to the schedule.

## Required privileges

The following users can resume a schedule:

- Members of the [`admin` role](/docs/v25.4/security-reference/authorization#default-roles). By default, the `root` user belongs to the `admin` role.
- Owners of a backup schedule, i.e., the user that [created the backup schedule](/docs/v25.4/create-schedule-for-backup).
- Owners of a changefeed schedule, i.e., the user that [created the changefeed schedule](/docs/v25.4/create-schedule-for-changefeed).

## Synopsis

<ResumeSchedules6 />

## Parameters

<table><thead><tr><th>Parameter</th><th>Description</th></tr></thead><tbody><tr><td><code>selectclause</code></td><td>A <a href="/docs/v25.4/selection-queries">selection query</a> that returns <code>id</code>(s) to resume.</td></tr><tr><td><code>scheduleID</code></td><td>The <code>id</code> of the schedule you want to resume, which can be found with <a href="/docs/v25.4/show-schedules"><code>SHOW SCHEDULES</code></a>.</td></tr></tbody></table>

## Examples

### Pause a schedule

```sql
> PAUSE SCHEDULE 589963390487363585;
```

```text
PAUSE SCHEDULES 1
```

### Resume a single schedule

```sql
> RESUME SCHEDULE 589963390487363585;
```

```text
RESUME SCHEDULES 1
```

### Resume multiple schedules

To resume multiple schedules, nest a [`SELECT` clause](/docs/v25.4/select-clause) that retrieves `id`(s) inside the `RESUME SCHEDULES` statement:

```sql
> RESUME SCHEDULES WITH x AS (SHOW SCHEDULES) SELECT id FROM x WHERE label = 'schedule_database';
```

```text
RESUME SCHEDULES 4
```

All schedules with the label `schedule_database` are resumed.

## See also

- [Manage a Backup Schedule](/docs/v25.4/manage-a-backup-schedule)
- [`BACKUP`](/docs/v25.4/backup)
- [`RESTORE`](/docs/v25.4/restore)
- [`SHOW BACKUP`](/docs/v25.4/show-backup)
- [`SHOW SCHEDULES`](/docs/v25.4/show-schedules)
- [`PAUSE SCHEDULES`](/docs/v25.4/pause-schedules)
- [`DROP SCHEDULES`](/docs/v25.4/drop-schedules)
- [`PAUSE JOB`](/docs/v25.4/pause-job)
- [`RESUME JOB`](/docs/v25.4/pause-job)
- [`CANCEL JOB`](/docs/v25.4/cancel-job)
- [Take Full and Incremental Backups](/docs/v25.4/take-full-and-incremental-backups)
- [Use the Built-in SQL Client](/docs/v25.4/cockroach-sql)
- [`cockroach` Commands Overview](/docs/v25.4/cockroach-commands)
