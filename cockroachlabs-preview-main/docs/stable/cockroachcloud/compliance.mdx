---
title: "Regulatory Compliance in CockroachDB Cloud"
old_description: "Learn about the regulatory and compliance standards met by CockroachDB advanced."
---

import Compliance from "/snippets/cockroachcloud/compliance.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "stable");
      });
    });
  }
  return null;
};

<PatchVersion />
<Compliance />
