---
title: "Connect to a CockroachDB Advanced Cluster"
old_description: "Learn how to connect and start interacting with your cluster."
---

import ConnectToAnAdvancedCluster from "/snippets/cockroachcloud/connect-to-an-advanced-cluster.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "stable");
      });
    });
  }
  return null;
};

<PatchVersion />
<ConnectToAnAdvancedCluster />
