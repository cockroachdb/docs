---
title: "Export Metrics From a CockroachDB Standard Cluster"
old_description: "Export Metrics From a CockroachDB Standard Cluster"
---

import ExportMetrics from "/snippets/cockroachcloud/export-metrics.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "stable");
      });
    });
  }
  return null;
};

<PatchVersion />
<ExportMetrics />
