---
title: "What's New in v26.2"
old_description: "Additions and changes in CockroachDB version v26.2 since version v26.1"
---

import V26_2 from "/snippets/releases/v26.2.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "stable");
      });
    });
  }
  return null;
};

<PatchVersion />
<V26_2 />
