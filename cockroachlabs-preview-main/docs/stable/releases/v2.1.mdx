---
title: "What's New in v2.1"
old_description: "Additions and changes in CockroachDB version v2.1 since version v2.0"
---

import V2_1 from "/snippets/releases/v2.1.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "stable");
      });
    });
  }
  return null;
};

<PatchVersion />
<V2_1 />
