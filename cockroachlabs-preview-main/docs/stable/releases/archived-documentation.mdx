---
title: "Archived Documentation"
old_description: "Documentation for CockroachDB versions that are no longer supported"
---

import ArchivedDocumentation from "/snippets/releases/archived-documentation.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "stable");
      });
    });
  }
  return null;
};

<PatchVersion />
<ArchivedDocumentation />
