---
title: "SHOW SCHEMAS"
old_description: "The SHOW SCHEMAS statement lists the schemas in a database."
---

import { ShowSchemas5 } from "/snippets/syntax-diagrams/ShowSchemas5.jsx";

The `SHOW SCHEMAS` [statement](/docs/v25.2/sql-statements) lists all [schemas](/docs/v25.2/sql-name-resolution#naming-hierarchy) in a database.

## Required privileges

The `CONNECT` [privilege](/docs/v25.2/security-reference/authorization#managing-privileges) on the database is required to list the schemas in a database.

## Synopsis

<ShowSchemas5 />

## Parameters

<table><thead><tr><th>Parameter</th><th>Description</th></tr></thead><tbody><tr><td><code>name</code></td><td>The name of the database for which to show <a href="/docs/v25.2/sql-name-resolution#naming-hierarchy">schemas</a>. When omitted, the schemas in the <a href="/docs/v25.2/sql-name-resolution#current-database">current database</a> are listed.</td></tr></tbody></table>

## Example

#### Setup

To follow along, run [`cockroach demo`](/docs/v25.2/cockroach-demo) to start a temporary, in-memory cluster with the [`movr`](/docs/v25.2/movr) sample dataset preloaded:

```shell
$ cockroach demo
```

### Show schemas in the current database

```sql
> CREATE SCHEMA org_one;
```

```sql
> SHOW SCHEMAS;
```

```text
     schema_name
----------------------
  crdb_internal
  information_schema
  org_one
  pg_catalog
  pg_extension
  public
(6 rows)
```

## See also

- [Logical Schemas and Namespaces](/docs/v25.2/sql-name-resolution)
- [`CREATE SCHEMA`](/docs/v25.2/create-schema)
- [`SHOW DATABASES`](/docs/v25.2/show-databases)
- [Information Schema](/docs/v25.2/information-schema)
- [SQL Statements](/docs/v25.2/sql-statements)
