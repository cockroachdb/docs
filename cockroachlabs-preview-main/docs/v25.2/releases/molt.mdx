---
title: "MOLT Releases"
old_description: "Changelog for MOLT Fetch, Verify, and Replicator"
---

import Molt from "/snippets/releases/molt.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v25.2");
      });
    });
  }
  return null;
};

<PatchVersion />
<Molt />
