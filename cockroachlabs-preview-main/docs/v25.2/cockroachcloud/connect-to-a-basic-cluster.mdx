---
title: "Connect to a CockroachDB Basic Cluster"
old_description: "Learn how to connect and start interacting with your Basic cluster."
---

import ConnectToABasicCluster from "/snippets/cockroachcloud/connect-to-a-basic-cluster.mdx";

export const PatchVersion = () => {
  if (typeof document !== "undefined") {
    requestAnimationFrame(() => {
      document.querySelectorAll("[data-versioned]").forEach(a => {
        a.href = a.href.replace("VERSION_PLACEHOLDER", "v25.2");
      });
    });
  }
  return null;
};

<PatchVersion />
<ConnectToABasicCluster />
