{
  "name": "cockroachlabs-docs-tests",
  "version": "0.0.1",
  "scripts": {
    "test": "bin/test"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-jest": "^21.2.0",
    "babel-preset-env": "^1.6.0",
    "fs-extra": "^9.1.0",
    "jest": "^21.2.1",
    "jest-cli": "^21.2.1"
  },
  "babel": {
    "presets": [
      "env"
    ]
  },
  "dependencies": {
    "front-matter": "^4.0.2"
  },
  "scripts": {
    "clear_jest": "jest --clearCache"
  }
}
